var text = "Make your variable names descriptive, so it's easier to figure out what your code means when you or someone else comes back to it three weeks or a year from now";
//First Logic
// var indexnum = text.indexOf("names");
// var ftext = text.slice(0,indexnum);
// var replace = "name";
// var ltext = text.slice(indexnum+5);
// document.write(ftext+replace+ltext);
//2nd Logic
// text = (text.slice(0,text.indexOf("names"))) + ("name") + text.slice(text.indexOf("names")+5);
// document.write(text);
//3rd logic
// text = text.replace("names","name");
// document.write(text)